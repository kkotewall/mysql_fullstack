// Import MySQL connection.
var connection = require("../config/connection.js");

// allow handlebars to see everything in db table so that it can display all the items on the html; the handlebars decides where it displays in the html